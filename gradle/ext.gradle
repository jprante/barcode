ext {
    user = 'jprante'
    projectName = 'barcode'
    projectDescription = 'Barcode generation library'
    scmUrl = "https://github.com/${user}/${name}"
    scmConnection = "scm:git:git://github.com/${user}/${projectName}.git"
    scmDeveloperConnection = "scm:git:git://github.com/${user}/${projectName}.git"
}
